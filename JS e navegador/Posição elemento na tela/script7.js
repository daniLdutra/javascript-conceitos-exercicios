// Checar a posição do elemento na tela: getBoundingClientRect nos da as posições de top, left, right, botton do elemento;

let elTeste = document.getElementById("titulo");

console.log(elTeste.getBoundingClientRect());
