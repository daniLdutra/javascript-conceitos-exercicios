// Remove tudo que não é string, inclusive espaços e caracteres especiais que pulam linha \n \n.
// Tratamento de dados com método trim.
let nome = "\n    Daniella \n";
let nomeCorrigido = nome.trim();
console.log(nome);
console.log(nomeCorrigido);
